import { NgModule } from '@angular/core';
import { StClientLibService } from './st-client-lib.service';

@NgModule({
  imports: [
  ],
  providers: [StClientLibService]
})
export class StClientLibModule { }
